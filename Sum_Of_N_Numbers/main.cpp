#include <iostream>

using namespace std;

int main()
{
    int N, Sum;
    cout<<"Enter the value of N ";
    cin>>N;
    Sum = N*(N+1)*0.5;
    cout<<"Sum of first N natural numbers is "<<Sum;
}
